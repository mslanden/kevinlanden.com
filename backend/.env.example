# Supabase Configuration (Required)
SUPABASE_URL=your-supabase-url
SUPABASE_KEY=your-supabase-anon-key

# JWT Secret (Required - CRITICAL FOR SECURITY)
# Generate a strong secret key for JWT signing
# You can generate one with: openssl rand -base64 32
JWT_SECRET=your_secure_jwt_secret_key_here_DO_NOT_USE_DEFAULT

# Server Configuration
PORT=3001
NODE_ENV=development

# Lofty CRM Configuration (Optional)
LOFTY_API_KEY=your-lofty-api-key
LOFTY_API_URL=https://api.lofty.com/v1.0